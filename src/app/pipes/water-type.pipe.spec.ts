import { WaterTypePipe } from './water-type.pipe';

describe('WaterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new WaterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
